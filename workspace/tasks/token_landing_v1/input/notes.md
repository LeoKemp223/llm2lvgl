# Token Landing Task Notes

- This is the first compatibility task for the new task-driven pipeline.
- It points at the existing `token_page.c` implementation so the new lint and export tools can be exercised before automatic code generation is added.
- The long-term goal is to replace this placeholder HTML with the real user-provided landing page source.

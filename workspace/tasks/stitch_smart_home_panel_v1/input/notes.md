# Stitch Smart Home Panel

- Source HTML copied from `stitch_smart_home_panel/code.html`.
- Source design notes copied from `input/source_design.md`.
- Task-local `reference/reference.png` is seeded from the provided source screenshot because the local HTML renderer is not installed in this environment.
- The task viewport is `480x480` and the runtime now launches the simulator at that size during validation.
- The conversion keeps the main visible dashboard state from the provided screenshot: top bar, thermostat card, lights card, security card, and bottom navigation.
- Material Symbols icon font is bundled under `input/assets/MaterialSymbolsOutlined.ttf` so the page can render the same icon names used by the source HTML.

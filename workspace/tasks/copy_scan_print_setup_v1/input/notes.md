# Copy Scan Print Setup

- Handcrafted LVGL page based on the provided screenshot.
- Viewport is fixed at `379x232` to match the source aspect.
- Implemented as a reference-only task so the generated C is not overwritten by the HTML pipeline.
